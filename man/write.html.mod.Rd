% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write.html.mod.R
\name{write.html.mod}
\alias{write.html.mod}
\title{Export a Data Frame to an HTML Table with Links and Images}
\usage{
write.html.mod(
  x,
  links,
  tiny.pic,
  tiny.pic.size = 50,
  title = "",
  file,
  digits = 3,
  col.align = "center",
  cellpadding = 10
)
}
\arguments{
\item{x}{A data frame to be converted into an HTML table.}

\item{links}{A list of character vectors of the same length as \code{ncol(x)},
where each element contains URLs corresponding to the rows of that column.}

\item{tiny.pic}{A list of character vectors of the same length as \code{ncol(x)},
specifying file paths or URLs for thumbnail images to display in cells.}

\item{tiny.pic.size}{A numeric value specifying the height and width (in pixels)
for the thumbnails. Default is 50.}

\item{title}{A character string used as the table caption title.}

\item{file}{A character string specifying the path/filename where the HTML
file will be written.}

\item{digits}{An integer indicating the number of decimal places for numeric columns. Default is 3.}

\item{col.align}{A character string for text alignment within cells (e.g., \code{'center'}, \code{'left'}, \code{'right'}). Default is \code{'center'}.}

\item{cellpadding}{A numeric value specifying the HTML table cell padding in pixels. Default is 10.}
}
\value{
The function returns no value; it writes an HTML file directly to disk.
}
\description{
Converts a data frame into a formatted HTML table, with options to imbed
hyperlinks, thumbnail images, control numeric rounding, and cell padding.
}
\examples{
\dontrun{
df <- data.frame(Gene = c("BRCA1", "TP53"), Value = c(1.2345, 0.9876))
write.html.mod(x = df, title = "My Summary Table", file = "summary.html")
}
}
